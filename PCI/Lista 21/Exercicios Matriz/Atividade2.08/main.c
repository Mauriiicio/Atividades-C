#include <stdio.h>
#include <stdlib.h>

int main()
{
    char biblioteca[8][40][150][200][35][65];
    for(int depart = 0; depart < 8; depart++)
        for(int estan = 0; estan < 40; estan++)
            for(int livro = 0; livro < 150; livro ++)
                for(int pag = 0; pag < 200; pag++)
                    for(int caracLinha = 0;caracLinha < 35; caracLinha++)
                        for(int caracColuna = 0; caracColuna < 65; caracColuna++){
                          char letra[][] = biblioteca[bibli][estan][livro][pag][caracLinha][linha];
                        }
}
